#include "philo.h"

//function hadle the cycle of each philo
void    ft_philo_cycle(t_philo *philo)
{
    //validate if any philo has died, if it so exit
    //start thinking
    //validate if any philo has died, if it so exit
    //pickup the forks if they are avaiable
    //validate if any philo has died, if it so exit
    //start eating
    //finish eat and start to sleep
}